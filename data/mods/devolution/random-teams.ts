import type {PRNG} from '../../../sim';
import RandomTeams, {MoveCounter} from '../../random-teams';

export class RandomDevolutionTeams extends RandomTeams {

}

export default RandomDevolutionTeams;
